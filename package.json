{
    "name": "connection-scoreboard",
    "version": "1.0.0",
    "description": "Root configuration for Connection Scoreboard",
    "scripts": {
        "install-all": "npm install && cd client && npm install && cd ../server && npm install",
        "start": "cd server && npm start",
        "dev": "concurrently \"cd server && node --watch server.js\" \"cd client && npm run dev\""
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.39.0"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}